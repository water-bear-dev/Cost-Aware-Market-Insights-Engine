# --- Environment Detection ---
# Set to 'production' to auto-switch to AWS Bedrock
ENVIRONMENT=local
# Log verbosity for backend + Developer Logs Console (DEBUG, INFO, WARNING, ERROR)
# If omitted: local -> DEBUG, production -> INFO
LOG_LEVEL=DEBUG

# --- LLM Configuration ---
# Options: mock, ollama, bedrock
# If ENVIRONMENT=production, this defaults to bedrock unless overridden
LLM_PROVIDER=ollama

# Ollama Settings (for local)
OLLAMA_URL=http://host.docker.internal:11434
OLLAMA_MODEL=llama3.2

# Bedrock Settings (for cloud)
AWS_DEFAULT_REGION=us-east-1

# --- Optional X Sentiment Source ---
# Set true only when X API credentials are configured
ENABLE_X_SENTIMENT=false
X_BEARER_TOKEN=
X_API_BASE_URL=https://api.x.com
X_SENTIMENT_MAX_RESULTS=15

# --- FinOps & Budget ---
DAILY_BUDGET_USD=5.00

# --- Market Data ---
TICKERS=AAPL,MSFT,GOOGL,AMZN,META

# --- Database ---
# Leave empty in production to use real DynamoDB
DYNAMODB_ENDPOINT_URL=http://dynamodb-local:8001
